<%- include('../partials/header') %>

<div class="container my-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow">
        <div class="card-header bg-danger text-white">
          <h3 class="mb-0">Thanh toán thất bại</h3>
        </div>
        <div class="card-body">
          <div class="text-center mb-4">
            <i class="bi bi-x-circle-fill text-danger" style="font-size: 4rem;"></i>
          </div>
          
          <h4 class="text-center mb-3">Thanh toán không thành công</h4>
          
          <p class="text-center mb-4">
            <%= message || 'Đã xảy ra lỗi trong quá trình thanh toán. Vui lòng thử lại hoặc chọn phương thức thanh toán khác.' %>
          </p>
          
          <div class="row mt-4">
            <div class="col-md-6">
              <div class="card mb-3">
                <div class="card-body">
                  <h5 class="mb-3">Thông tin đơn hàng</h5>
                  <% if (order) { %>
                    <p><strong>Mã đơn hàng:</strong> #<%= order.id %></p>
                    <p><strong>Tổng tiền:</strong> <%= new Intl.NumberFormat('vi-VN', { style: 'currency', currency: 'VND' }).format(order.total_amount) %></p>
                    <p><strong>Trạng thái:</strong> <span class="badge bg-danger">Thanh toán thất bại</span></p>
                  <% } else { %>
                    <p>Không tìm thấy thông tin đơn hàng.</p>
                  <% } %>
                </div>
              </div>
            </div>
            <!-- <div class="col-md-6">
              <div class="card">
                <div class="card-body">
                  <h5 class="mb-3">Các lựa chọn</h5>
                  <p>Bạn có thể thử lại thanh toán hoặc chọn phương thức thanh toán khác.</p>
                  
                  <% if (order) { %>
                    <div class="d-grid gap-2">
                      <a href="/gio-hang/checkout?retry=<%= order.id %>" class="btn btn-primary">
                        <i class="bi bi-arrow-clockwise"></i> Thử lại thanh toán
                      </a>
                      <a href="/gio-hang/checkout?change_payment=<%= order.id %>" class="btn btn-outline-primary">
                        <i class="bi bi-wallet2"></i> Đổi phương thức thanh toán
                      </a>
                    </div>
                  <% } %>
                </div>
              </div>
            </div> -->
          </div>
          
          <div class="text-center mt-4">
            <a href="/" class="btn btn-secondary me-2">
              <i class="bi bi-house-fill"></i> Về trang chủ
            </a>
            <a href="/gio-hang" class="btn btn-outline-secondary">
              <i class="bi bi-cart"></i> Xem giỏ hàng
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<%- include('../partials/footer') %> 